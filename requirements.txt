# === Core ===
streamlit==1.38.0
Pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
python-Levenshtein>=0.21.0

# === OCR Engines ===
# 1. PaddleOCR (Arabic + English, best for mixed medical docs)
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# 2. EasyOCR (80+ languages, good for Latin and mixed text)
easyocr>=1.7.0

# 3. Tesseract (fast, reliable for printed text)
pytesseract>=0.3.10

# 4. TrOCR (Transformer-based, best for handwriting)
transformers>=4.35.0
torch>=2.0.0
sentencepiece>=0.1.99

# 5. Surya OCR (modern, 90+ languages, high accuracy)
# IMPORTANT: Pinned to 0.6.4 — newer versions (0.7+) restructured the API
# and removed surya.detection / surya.recognition / surya.model.* modules
surya-ocr==0.6.4

# === Utilities ===
# python-Levenshtein speeds up Levenshtein distance in ensemble merging
# matplotlib is used for charts in the statistics tab
